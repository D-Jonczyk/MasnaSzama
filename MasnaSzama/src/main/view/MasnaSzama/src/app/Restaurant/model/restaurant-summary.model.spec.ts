import { RestaurantSummary } from './restaurant-summary.model';

describe('RestaurantSummary', () => {
  it('should create an instance', () => {
    expect(new RestaurantSummary()).toBeTruthy();
  });
});
